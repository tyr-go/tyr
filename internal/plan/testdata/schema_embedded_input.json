{
  "schema": {
    "$ref": "#/$defs/embedded"
  },
  "$defs": {
    "embedded": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "integer",
          "maximum": 100
        },
        "note": {
          "type": "string",
          "minLength": 1
        },
        "own": {
          "type": "string"
        }
      },
      "additionalProperties": {
        "type": "integer"
      }
    }
  }
}
