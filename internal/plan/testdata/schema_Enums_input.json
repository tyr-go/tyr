{
  "schema": {
    "$ref": "#/$defs/EnumsInput"
  },
  "$defs": {
    "EnumNestedInput": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/Status"
        }
      }
    },
    "EnumsInput": {
      "type": "object",
      "properties": {
        "status": {
          "$ref": "#/$defs/Status"
        },
        "ptr": {
          "anyOf": [
            {
              "$ref": "#/$defs/Status"
            },
            {
              "type": "null"
            }
          ]
        },
        "priority": {
          "$ref": "#/$defs/Priority"
        },
        "size": {
          "$ref": "#/$defs/Size"
        },
        "list": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Status"
          }
        },
        "pair": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Priority"
          },
          "minItems": 2,
          "maxItems": 2
        },
        "by_status": {
          "type": "object",
          "additionalProperties": {
            "type": "integer"
          },
          "propertyNames": {
            "$ref": "#/$defs/Status"
          }
        },
        "sizes": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Size"
          }
        },
        "nested": {
          "$ref": "#/$defs/EnumNestedInput"
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnumNestedInput"
          }
        },
        "required": {
          "minLength": 1,
          "allOf": [
            {
              "$ref": "#/$defs/Status"
            }
          ]
        }
      },
      "required": [
        "required"
      ]
    },
    "Priority": {
      "type": "integer",
      "enum": [
        1,
        2,
        3
      ]
    },
    "Size": {
      "type": "string",
      "enum": [
        "small",
        "medium",
        "large"
      ]
    },
    "Status": {
      "type": "string",
      "enum": [
        "todo",
        "doing",
        "done"
      ]
    }
  }
}
